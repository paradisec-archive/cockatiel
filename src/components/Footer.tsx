export const Footer = () => {
  return (
    <footer className="border-t border-border mt-8">
      <div className="mx-auto max-w-6xl px-4 py-3 font-mono text-xs text-muted-foreground">
        All processing runs locally in your browser — no audio leaves your device.
      </div>
    </footer>
  );
};
