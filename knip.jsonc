{
  "$schema": "https://unpkg.com/knip@5/schema.json",
  "tags": ["-lintignore"],
  "ignore": [
    // shadcn/ui component library
    "src/components/ui/**",
    // Ambient type augmentations (global `declare` blocks aren't reachable via imports)
    "**/*.d.ts"
  ],
  "commitlint": {
    "entry": ["commitlint.config.ts"]
  }
}
